





handleDecrement = (habit) => {
    const habits = [...this.state.habits]
    const count = habits.indexOf(count) -1
    habits.count = count <0? 0 :count
    habits[index].count = count < 0 ? 0 : count;
    this.setState({ habits });

}